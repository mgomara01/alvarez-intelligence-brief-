# Allow everything by default — let Google crawl the whole public site.
User-agent: *
Allow: /

# Block nothing public. If you add an admin/dashboard later, disallow it here:
# Disallow: /admin/

# Point crawlers at the sitemap (generated by @astrojs/sitemap at build time).
# Update this host if your production domain differs.
Sitemap: https://www.alvarezplumbing.com/sitemap-index.xml
